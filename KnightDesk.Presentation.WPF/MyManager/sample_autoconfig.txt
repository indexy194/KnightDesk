# Auto System Configuration Sample - Multiple Client Support
# Generated: 2024-12-19
# Copy this file to autoconfig.txt and modify as needed

# TCP Settings - Controls how the game receives login data
TcpLoginEnabled=true          # Enable TCP login from external tool
FileLoginEnabled=false        # Enable file-based login (login.txt)
TcpAutoStart=true            # Auto start TCP server when game starts
TcpPort=5555                 # Base port for TCP communication (will use 5555, 5556, 5557...)
TcpMaxClients=3              # Maximum number of simultaneous game clients

# Auto Features - Enable/disable various auto functions
AutoHuntingEnabled=false     # Auto hunting/attacking monsters
AutoMiningEnabled=false      # Auto mining
AutoPhoBanEnabled=false      # Auto Pho Ban dungeon
AutoThuCuoiEnabled=false     # Auto Thu Cuoi (mount usage)
AutoEventEnabled=false       # Auto event participation
AutoChangeEquipEnabled=false # Auto equipment changes

# Auto Tool Settings - Controlled by Management App
AutoToolEnabled=false        # Auto tool state (controlled by TCP)
AutoEventToolEnabled=false   # Auto event tool (controlled by TCP)
AutoEventType=Trung Thu      # Event type for auto event tool
AutoEquipToolEnabled=false   # Auto equip tool (controlled by TCP)
AutoEquipType=Sát Thủ        # Equipment type for auto equip tool
FocusName=                   # Focus target name (controlled by TCP)

# Auto Delays (in game ticks - 1 second ≈ 60 ticks)
AutoMoveDelay=500           # Delay before switching target when idle (default: 500 ticks ≈ 8.3 seconds)
AutoRespawnDelay=150        # Delay before auto respawn when dead (default: 150 ticks ≈ 2.5 seconds)
AutoMineDelay=10            # Delay between mining attempts (default: 10 ticks ≈ 0.17 seconds)
AutoPhoBanMoveDelay=50      # Delay for movement in Pho Ban (default: 50 ticks ≈ 0.83 seconds)
AutoPhoBanAttackDelay=200   # Delay before attacking in Pho Ban (default: 200 ticks ≈ 3.3 seconds)

# Auto HP/MP Settings
AutoHPMPEnabled=false       # Enable auto HP/MP potion usage
AutoHPPercentage=50         # Use HP potion when HP drops below this percentage
AutoMPPercentage=50         # Use MP potion when MP drops below this percentage

# Auto Item Settings
AutoGetItemEnabled=false    # Enable auto item pickup
AutoItemColorLevel=-1       # Minimum item color level to pick up (-1=disabled, 0=white, 1=green, 2=blue, 3=purple, 4=orange)
AutoItemMoneyType=0         # Money pickup type (0=none, 1=coins, 2=all)
AutoItemPotionType=0        # Potion pickup type (0=none, 1=HP, 2=MP, 3=both)

# Auto Buff Settings
AutoBuffEnabled=false       # Enable auto buff usage

# MULTIPLE CLIENT SETUP INSTRUCTIONS:
# ===================================
# 1. Each game client will automatically listen on different ports:
#    - Client 1: Port 5555 (TcpPort)
#    - Client 2: Port 5556 (TcpPort + 1)
#    - Client 3: Port 5557 (TcpPort + 2)
#    - etc. up to TcpMaxClients
#
# 2. Management App will automatically:
#    - Send login commands to available ports
#    - Track which account is using which port
#    - Send tool commands to the correct client port
#
# 3. Each game client operates independently:
#    - Has its own TCP listener on a unique port
#    - Receives commands specific to its account
#    - Maintains separate auto tool states
#
# 4. No manual port configuration needed - everything is automatic!

# USAGE:
# ======
# 1. Start multiple game clients (up to TcpMaxClients)
# 2. Each will listen on sequential ports automatically
# 3. Use Management App to login each account
# 4. Control each account's auto tools independently
# 5. All commands are sent to the correct client automatically 