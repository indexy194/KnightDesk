<Window x:Class="KnightDesk.Presentation.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:KnightDesk.Presentation.WPF.Views"
        mc:Ignorable="d"
        Title="LoginWindow" 
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Height="400"
        Width="600">
    <Grid>
        <!--Background-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border CornerRadius="25 0 0 25" Width="250">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#343155" Offset="0" />
                        <GradientStop Color="#3B2E58" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <Border CornerRadius="0 25 25 0" MouseDown="Border_MouseDown" Grid.Column="1">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#3E3A65" Offset="0" />
                        <GradientStop Color="#473765" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
            </Border>
            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal" Margin="0 50 0 0" HorizontalAlignment="Center">
                    <fa:ImageAwesome Icon="CheckCircle" Foreground="White" Width="30"/>
                    <TextBlock Text="KnightDesk" FontWeight="Bold" FontSize="25" VerticalAlignment="Center" Margin="10 0 0 0" Foreground="#FFFFFF"/>
                </StackPanel>
            </StackPanel>
            <!--login form -->
            <StackPanel Grid.Column="1">
                <!-- close button-->
                <fa:ImageAwesome Icon="Close" Foreground="White" Width="15" HorizontalAlignment="Right" Margin="0 10 10 0" Cursor="Hand" MouseDown="CloseButton_MouseDown"/>
                <!--title-->
                <TextBlock Text="Đăng nhập" FontWeight="Bold" FontSize="25" HorizontalAlignment="Center" Margin="0 50 0 0" Foreground="#FFFFFF"/>
                <!-- username -->
                <StackPanel Margin="50 30 50 0" Orientation="Horizontal">
                    <fa:ImageAwesome Icon="User" Foreground="White" Width="20" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             Height="35" 
                             Width="200"
                             FontSize="14" 
                             Padding="10" 
                             Margin="15 0 0 0" 
                             BorderThickness="0" 
                             Background="#5A5470" 
                             Foreground="White" />
                </StackPanel>
                <!-- password -->
                <StackPanel Margin="50 20 50 0" Orientation="Horizontal">
                    <fa:ImageAwesome Icon="Key" Foreground="White" Width="20" VerticalAlignment="Center"/>
                    <PasswordBox x:Name="pbPassword"
                                 Style="{StaticResource pwBox}"
                                Height="35" 
                                Width="200"
                                FontSize="14" 
                                Padding="10"
                                Margin="15 0 0 0"
                                BorderThickness="0" 
                                Background="#5A5470" 
                                Foreground="White"
                                PasswordChanged="PasswordBox_PasswordChanged" />
                </StackPanel>
                
                <!-- Remember Me Checkbox -->
                <CheckBox IsChecked="{Binding RememberMe}"
                          Content="Ghi nhớ đăng nhập"
                          Foreground="White"
                          FontSize="12"
                          HorizontalAlignment="Center"
                          Margin="0 15 0 0"
                          Cursor="Hand">
                    <CheckBox.Style>
                        <Style TargetType="CheckBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="CheckBox">
                                        <StackPanel Orientation="Horizontal">
                                            <Border x:Name="checkBorder" 
                                                    Width="16" 
                                                    Height="16" 
                                                    Background="#5A5470" 
                                                    BorderBrush="White" 
                                                    BorderThickness="1" 
                                                    CornerRadius="2"
                                                    Margin="0 0 8 0">
                                                <fa:ImageAwesome x:Name="checkMark" 
                                                                 Icon="Check" 
                                                                 Foreground="White" 
                                                                 Width="10" 
                                                                 Height="10" 
                                                                 Visibility="Collapsed"/>
                                            </Border>
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                                <Setter TargetName="checkBorder" Property="Background" Value="#3E3A65"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#CCCCCC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </CheckBox.Style>
                </CheckBox>
                
                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="#FF6B6B" 
                           FontSize="12" 
                           HorizontalAlignment="Center" 
                           Margin="0 10 0 0"
                           TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasError}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <!-- login button -->
                <Button Command="{Binding LoginCommand}"
                        Width="100" 
                        Height="30" 
                        Margin="0 20 0 0" 
                        HorizontalAlignment="Center"
                        Style="{StaticResource loadButton}"
                        FontSize="15"
                        Cursor="Hand"
                        IsEnabled="{Binding CanLogin}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Login">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="...">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

    </Grid>
</Window>
